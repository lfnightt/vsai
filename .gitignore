# ── Project-specific excludes (per user request) ──────────────────
# node_modules is now installed by npm in the Docker build
node_modules/
package-lock.json
VS-AI.zip

# ── Secrets & local configuration ────────────────────────────────
.env
.env.*
*.local
config.php

# ── OS & editor files ────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ── Windows batch scripts (local only, not for version control) ──
*.bat
